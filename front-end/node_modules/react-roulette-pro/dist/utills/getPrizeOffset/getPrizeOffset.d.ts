/**
 * This function gets an offset from container start to container center
 */
declare const getPrizeOffset: (itemSize: number, prizeIndex: number, containerCenter: number) => number;
export default getPrizeOffset;
